providers = ["python"]

[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = ["python -m venv /app/.venv", ". /app/.venv/bin/activate && pip install -r requirements.txt"]

[start]
cmd = ". /app/.venv/bin/activate && daphne -b 0.0.0.0 -p $PORT chatappp.asgi:application"
